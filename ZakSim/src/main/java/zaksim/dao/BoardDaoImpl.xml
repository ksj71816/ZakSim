<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
  <mapper namespace="zaksim.dao.BoardDao"> 
  
  
  <resultMap type="zaksim.dto.Board" id="CitationList">
  
  	<result column="rnum" property="rnum"/>
  	<result column="idx" property="idx"/>
  	<result column="challenge_idx" property="challenge_idx"/>
  	<result column="writer_idx" property="writer_idx"/>
  	<result column="title" property="title"/>
  	<result column="content" property="content"/>
  	<result column="image" property="image"/>
  	<result column="written_date" property="written_date"/>
  	<result column="hit" property="hit"/>
  	
  </resultMap>
  
  
  
  
  
  
  
  
  <select id="selectSeq" resultType="int">
  
 	 select board_seq.nextval from dual
  
  </select>
  
  
  <insert id="insertCitation" parameterType="zaksim.dto.Board">
  
  	INSERT INTO BOARD VALUES (#{idx}, #{challenge_idx}, null, #{writer_idx}, #{title}, #{content}, #{image}, 1, #{certification}, sysdate, #{hit})
  
  </insert>
  
  
  <select id="selectTotalCitation" resultMap="CitationList">
  
  	select idx, challenge_idx, writer_idx, title, content, image, written_date, hit  from board where certification=1 ORDER BY idx desc
  
  </select>
  
  <select id="cntTotal" resultType="int">
  
  	SELECT count(*) FROM board where certification=1
  
  </select>
  
  <select id="selectPagingCitation" parameterType="zaksim.util.Paging" resultMap="CitationList">
  
  	select * from ( select rownum rnum, B.* from 
    		( select idx, challenge_idx, written_date, title, content, image, writer_idx, hit from board where certification=1 order by idx desc) B order by rnum) where rnum between #{startNo} and #{endNo}
  
  </select>
  
  
  
  
  
  
  </mapper>
  