<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="zaksim.dao.CommunityEditDAO">



	<select id="getCategoryIdx" resultType="int" parameterType="int">
		select * from CommunityCategory where idx=#{idx}
	</select>


	<insert id="createGroup"
		parameterType="zaksim.dto.CommunityGroup">
		INSERT INTO CommunityGroup (idx, member_idx, category_idx, title, content,
		image, storedName, max, open_date, secret, password)
		VALUES (#{idx}, #{member_idx}, #{category_idx}, #{title}, #{content}, #{image}, #{storedName}
		#{max}, default, #{secret}, #{password})
	</insert>
	
	<insert id="addMember" parameterType="zaksim.dto.GroupMember">
			insert into GroupMember (idx, group_idx, member_idx) values (groupmember_SEQ.nextval, #{group_idx } , #{member_idx } )		
	</insert>
	
	
	<select id="getnextval" resultType="int">
		select communitygroup_seq.nextval from dual
	</select>


	<insert id="createKeyword"
		parameterType="zaksim.dto.GroupKeyword">
		INSERT INTO groupkeyword (idx, group_idx, keyword)
		VALUES (groupkeyword_SEQ.nextval, #{group_idx}, #{keyword})
	</insert>
	
	<update id="updateGroup" parameterType="zaksim.dto.CommunityGroup">
		update communityGroup set category_idx=#{category_idx }, title=#{title }, content=#{content }, secret= #{secret },
		<if test="secret == 1">
			password = #{password },
		</if>
		<if test="secret == 0">
			password = null,
		</if>
		image=#{image }, storedName=#{storedName}, max=#{max }  where idx=#{idx }
	</update>
	
	<delete id="deleteKeyword" parameterType="int">
		delete from groupkeyword where group_idx=#{group_idx }
 	</delete>
 	
 	<delete id="deleteGroup" parameterType="int">
 		delete from communityGroup where idx=#{idx }
 	</delete>
 	
 	<delete id="deleteGroupMember" parameterType="int">
 		delete from groupMember where idx=#{idx }
 	</delete>
 	
 	<select id="like" parameterType="zaksim.dto.GroupLike" resultType="int">
  		select count(*) from grouplike where  group_idx=#{group_idx } and member_idx=#{member_idx }
  	</select>

	<insert id="likeGroup" parameterType="zaksim.dto.GroupLike">
		insert into GroupLike (idx, group_idx, member_idx)
		VALUES (GroupLike_SEQ.nextval, #{group_idx}, #{member_idx})
	</insert>
	
	<delete id="disLikeGroup" parameterType="zaksim.dto.GroupLike">
		delete from groupLike where group_idx = #{group_idx } and member_idx =#{member_idx }
	</delete>
	
<!-- 	public void joinedMember(GroupMember groupMember) -->
	<insert id="joinedMember" parameterType="zaksim.dto.GroupMember">
		insert into GroupMember (idx, group_idx, member_idx)
		VALUES (GroupMember_SEQ.nextval, #{group_idx}, #{member_idx})
	</insert>
	
	
</mapper>
  
